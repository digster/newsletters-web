# 2026-03-13: Add Input Directory CLI Argument

## What Changed
- Added `argparse` CLI argument parsing to `scripts/build_site.py`
- New positional optional `input_dir` argument (`nargs="?"`) defaults to existing `../newsletters/` path
- Resolved parsed path to absolute `Path` and threaded it through `collect_emails()` and `copy_html_emails()` calls
- No changes to any other files — functions already accepted `source_dir` as a parameter

## Files Modified
- `scripts/build_site.py` — Added `import argparse`, argument parser in `main()`, replaced module-level `SOURCE_DIR` usage with parsed CLI value
