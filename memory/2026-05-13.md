# 2026-05-13

## Hover preview delay tuned to 1.4 s

Lowered `PREVIEW_HOVER_DELAY_MS` from `2000` to `1400` in both `app.js` (root)
and `templates/app.js`. The two copies are byte-identical and hand-kept in
sync; verified post-edit with `diff -q`. No other call sites needed updating —
the constant is consumed once each at line 692 (`setTimeout(...,
PREVIEW_HOVER_DELAY_MS)`). Removed the stale `// per user spec: "more than two
seconds"` comment since 1.4 s no longer fits that description.

Note for future work: the dual-copy `app.js` layout is fragile (no build step,
no symlink). Worth collapsing eventually.
